[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -m "not integration"
markers =
    integration: hits real local binaries (nmap/dumpcap/tshark) against 127.0.0.1 — slow/flaky under load, opt-in only (run with: pytest -m integration)
